{
	"name": "thyn",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
		"check": "tsc"
	},
	"dependencies": {
		"kaplay": "^3001.0.9"
	},
	"devDependencies": {
		"@types/node": "^22.10.10",
		"typescript": "^5.7.3",
		"vite": "^6.0.11"
	}
}
